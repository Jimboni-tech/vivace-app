const express = require('express');
const router = express.Router();

// Placeholder for piece routes
router.get('/', (req, res) => {
  res.json({ message: 'Piece routes coming soon' });
});

module.exports = router;
