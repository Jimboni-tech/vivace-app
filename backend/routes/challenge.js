const express = require('express');
const router = express.Router();

// Placeholder for challenge routes
router.get('/', (req, res) => {
  res.json({ message: 'Challenge routes coming soon' });
});

module.exports = router;
