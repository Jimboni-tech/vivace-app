const express = require('express');
const router = express.Router();

// Placeholder for social routes
router.get('/', (req, res) => {
  res.json({ message: 'Social routes coming soon' });
});

module.exports = router;
